#include <string>
#include <vector>
#include <queue>

using namespace std;

vector<int> solution(vector<string> operations) {
    vector<int> answer;
    



    return answer;
}

int main(){
    vector<string> s;
    solution(s);
    return 0;
}