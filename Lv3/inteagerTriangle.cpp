#include <string>
#include <vector>

using namespace std;

int solution(vector<vector<int>> triangle) {
    int answer = 0;
    return answer;
}

int main(){
    vector<vector<int>> triangle;
    solution(triangle);
    return 0;
}